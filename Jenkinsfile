node{
    stage('Gradle Build'){
        echo "Build Started"
        sudo apt-get install
        sudo apt-get update
        sudo apt-get install gradle
        sh "gradle :assemble"
        echo "Build Done"
    }
}
